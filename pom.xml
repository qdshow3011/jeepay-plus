<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion> <!-- POM模型版本 -->

  <groupId>com.openhubs</groupId> <!-- 组织名, 类似于包名 -->
  <artifactId>OpenHubs Pay</artifactId>  <!-- 项目名称  -->
  <packaging>pom</packaging> <!-- 项目的最终打包类型/发布形式, 可选[jar, war, pom, maven-plugin]等 -->

  <name>OpenHubs Pay</name>
  <version>Final</version> <!-- pom版本号/项目总版本号， 每个子项目引入的版本号必须一致。  最外层的pom.xml版本号保持不变，始终为Final版本。 更新版本请更改isys.version属性  -->
  <description>OpenHubs Pay计全支付系统</description> <!-- 项目描述 -->
  <url>https://www.openhubs.com</url>

  <!-- 继承：Spring Boot Parent -->
  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.5.11</version>
  </parent>

  <!-- 声明子项目 -->
  <modules>
    <module>OpenHubs Pay-z-codegen</module>    <!-- 代码生成器 -->

    <module>OpenHubs Pay-core</module>  <!-- 基础函数, 包含工具类等 -->
    <module>OpenHubs Pay-service</module>  <!-- db service等 -->

  	<module>OpenHubs Pay-manager</module>	<!-- 运营平台管理端 -->
  	<module>OpenHubs Pay-merchant</module>	<!-- 商户平台管理端 -->
    <module>OpenHubs Pay-payment</module>	<!-- 支付统一网关 -->

    <module>OpenHubs Pay-components</module> <!-- 组件包 -->
  </modules>

  <!-- 配置属性声明, 支持自定义参数 -->
  <properties>

    <isys.version>2.4.0</isys.version> <!-- 指定当前[项目]版本号 -->
    <projectRootDir>${basedir}</projectRootDir>
    <java.version>17</java.version> <!-- Spring Boot 3.5 最低要求 Java 17 -->
    <maven.compiler.parameters>true</maven.compiler.parameters>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding> <!-- 项目构建输出编码 -->

    <!-- 其他工具包 -->
    <OpenHubs Pay.sdk.java.version>pls-1.3.0</OpenHubs Pay.sdk.java.version>
    <fastjson.version>1.2.83</fastjson.version> <!-- fastjson -->
    <mybatis.plus.starter.version>3.5.16</mybatis.plus.starter.version>  <!-- mybatis plus Boot 3 -->
    <hutool.util.version>5.8.26</hutool.util.version>  <!-- hutool -->
    <jjwt.version>0.13.0</jjwt.version>
    <binarywang.weixin.java.version>4.6.0</binarywang.weixin.java.version>
    <druid.version>1.2.27</druid.version>

  </properties>


  <!-- 依赖包管理， 按需添加 -->
  <dependencyManagement>
    <dependencies>

        <!-- 依赖 [ core ]包。  -->
        <dependency>
            <groupId>com.openhubs</groupId>
            <artifactId>OpenHubs Pay-core</artifactId>
            <version>${isys.version}</version>
        </dependency>

        <!-- [ service ]包, 会自动传递依赖[ core ]包。  -->
        <dependency>
            <groupId>com.openhubs</groupId>
            <artifactId>OpenHubs Pay-service</artifactId>
            <version>${isys.version}</version>
        </dependency>

        <!-- [ oss ]包  -->
        <dependency>
            <groupId>com.openhubs</groupId>
            <artifactId>OpenHubs Pay-components-oss</artifactId>
            <version>${isys.version}</version>
        </dependency>

        <!-- [ mq ]包  -->
        <dependency>
            <groupId>com.openhubs</groupId>
            <artifactId>OpenHubs Pay-components-mq</artifactId>
            <version>${isys.version}</version>
        </dependency>

        <!-- OpenHubs Pay sdk 支付工具包 -->
        <dependency>
            <groupId>com.openhubs</groupId>
            <artifactId>OpenHubs Pay-sdk-java</artifactId>
            <version>${OpenHubs Pay.sdk.java.version}</version>
        </dependency>

        <!-- alibaba FastJSON -->
        <dependency>
            <groupId>com.alibaba</groupId>
            <artifactId>fastjson</artifactId>
            <version>${fastjson.version}</version>
        </dependency>

        <!-- JWT -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>${jjwt.version}</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>${jjwt.version}</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>${jjwt.version}</version>
        </dependency>

        <!--wx_pay  https://github.com/wechat-group/WxJava  -->
        <dependency>
            <groupId>com.github.binarywang</groupId>
            <artifactId>weixin-java-pay</artifactId>
            <version>${binarywang.weixin.java.version}</version>
        </dependency>

        <dependency>
            <groupId>com.github.binarywang</groupId>
            <artifactId>weixin-java-mp</artifactId>
            <version>${binarywang.weixin.java.version}</version>
        </dependency>

        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
            <version>${mybatis.plus.starter.version}</version>
        </dependency>
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-jsqlparser</artifactId>
            <version>${mybatis.plus.starter.version}</version>
        </dependency>

        <!-- 生成二维码依赖 -->
        <dependency>
            <groupId>com.google.zxing</groupId>
            <artifactId>core</artifactId>
            <version>3.1.0</version>
        </dependency>

        <dependency>
            <groupId>com.google.zxing</groupId>
            <artifactId>javase</artifactId>
            <version>3.1.0</version>
        </dependency>

        <!-- https://mvnrepository.com/artifact/com.alipay.sdk/alipay-sdk-java -->
        <dependency>
            <groupId>com.alipay.sdk</groupId>
            <artifactId>alipay-sdk-java</artifactId>
            <version>4.38.61.ALL</version>
        </dependency>

        <!-- 阿里云oss组件  -->
        <dependency>
            <groupId>com.aliyun.oss</groupId>
            <artifactId>aliyun-sdk-oss</artifactId>
            <version>3.13.0</version>
        </dependency>

        <!-- Knife4j  -->
        <dependency>
            <groupId>com.github.xiaoymin</groupId>
            <artifactId>knife4j-openapi3-jakarta-spring-boot-starter</artifactId>
            <version>4.5.0</version>
        </dependency>

        <!-- swagger注解包 -->
        <dependency>
            <groupId>io.swagger</groupId>
            <artifactId>swagger-annotations</artifactId>
            <version>1.6.6</version>
        </dependency>

    </dependencies>

  </dependencyManagement>

  <!-- 所有项目 项目依赖声明 -->
  <dependencies>

      <dependency>
          <groupId>org.projectlombok</groupId>
          <artifactId>lombok</artifactId>
          <optional>true</optional>
          <scope>provided</scope> <!-- 编译阶段生效，不需要打入包中 -->
      </dependency>

      <!-- https://mvnrepository.com/artifact/cn.hutool/hutool-all -->
      <dependency>
          <groupId>cn.hutool</groupId>
          <artifactId>hutool-all</artifactId>
          <version>${hutool.util.version}</version>
      </dependency>
  </dependencies>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>com.google.cloud.tools</groupId>
                    <artifactId>jib-maven-plugin</artifactId>
                    <version>3.2.1</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>


</project>
