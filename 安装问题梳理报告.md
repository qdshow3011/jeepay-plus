# OpenHubs PAY 项目安装问题梳理与修复报告

> 仓库：https://github.com/qdshow3011/jeepay-plus.git
> 版本：v2.4.0_20250612
> 修复日期：2026-07-21

---

## 项目概览

开算支付 (OpenHubs PAY) 是一套聚合支付系统，采用 **Java 17 + Spring Boot + MyBatis-Plus** 后端 + **Vue 3 + Vite + Ant Design Vue 4** 前端架构。依赖中间件包括 **MySQL 8.4、Redis 7.4、ActiveMQ 5.16.4**。整个项目通过 **Docker Compose** 编排部署。

---

## 一、已修复的严重问题（P0）

### 1. ✅ `jeepay-z-codegen` 模块架构不兼容 — 已修复

**文件**: `jeepay-z-codegen/pom.xml`

| 问题 | 修复内容 |
|------|----------|
| 无父 POM | 添加 `<parent>` 引用 `com.jeequan:jeepay:Final` |
| Java 版本混乱 | 移除矛盾的 `java.version=1.8`，统一由 compiler plugin `release=17` 控制 |
| 过时依赖 | `mysql-connector-java` → `mysql-connector-j`（Jakarta 兼容） |
| spring-context 版本 | 移除硬编码版本号，由父 POM 的 Spring Boot BOM 管理 |
| mybatis-plus-generator | 升级 3.3.0 → 3.5.9（Jakarta 兼容） |

---

### 2. ✅ 9 个文件 UTF-8 BOM 编码 — 已修复

已移除以下文件的 BOM 头（`EF BB BF`）：

- `.env.example`
- `docker-compose.yml`
- `docker/mysql/Dockerfile`
- `docker/activemq/Dockerfile`
- `.mvn/settings.xml`
- `jeepay-ui/Dockerfile`
- `conf/manager/application.yml`
- `conf/payment/application.yml`
- `conf/merchant/application.yml`

---

### 3. ✅ UI Dockerfile `ENV PORT` 语法错误 — 已修复

**文件**: `jeepay-ui/Dockerfile`

```dockerfile
# 修复前（Docker 不支持 shell 参数展开）
ENV PORT=${PORT:-80}
EXPOSE ${PORT}
HEALTHCHECK ... wget http://localhost:${PORT}/

# 修复后
ENV PORT=80
EXPOSE 80
HEALTHCHECK ... curl http://localhost:80/
```

同时将 Healthcheck 中的 `wget` 替换为 `curl`（nginx:alpine 内置支持）。

---

## 二、已修复的中等问题（P1）

### 4. ✅ MySQL Healthcheck 依赖业务表 — 已修复

**文件**: `docker-compose.yml`

```yaml
# 修复前
test: ["CMD-SHELL", "MYSQL_PWD=$${MYSQL_ROOT_PASSWORD} mysql -uroot -e \"SELECT 1 FROM $${MYSQL_DATABASE}.t_sys_config LIMIT 1\" || exit 1"]

# 修复后
test: ["CMD-SHELL", "MYSQL_PWD=$${MYSQL_ROOT_PASSWORD} mysql -uroot -e \"SELECT 1\" || exit 1"]
```

现在 healthcheck 只检查 MySQL 服务是否可用，不依赖 init.sql 创建的表。

---

### 5. ✅ Nginx 镜像 healthcheck 使用 wget — 已修复

**文件**: `docker-compose.yml`

```yaml
# 修复前（nginx:alpine 没有 wget）
test: ["CMD-SHELL", "wget -q --spider http://localhost:80/ || exit 1"]

# 修复后
test: ["CMD", "curl", "-f", "-s", "http://localhost:80/"]
```

三个 UI 服务（ui-payment、ui-manager、ui-merchant）已全部修复。

---

### 6. ✅ `docker/rocketmq` 废弃目录 — 已清理

已删除 `docker/rocketmq/` 目录及其内容。

---

### 7. ✅ 开发配置硬编码明文密码 — 已修复

**文件**: `conf/devCommons/config/application.yml`

| 配置项 | 修复前 | 修复后 |
|--------|--------|--------|
| MySQL 地址 | `127.0.0.1:3306/jeepaydb` | `${MYSQL_HOST:127.0.0.1}:3306/${MYSQL_DATABASE:jeepaydb}` |
| MySQL 用户/密码 | `root` / `123` | `${MYSQL_USER:root}` / `${MYSQL_PASSWORD:}` |
| Redis 地址 | `127.0.0.1` | `${REDIS_HOST:127.0.0.1}` |
| ActiveMQ 地址 | `127.0.0.1` | `${ACTIVEMQ_HOST:127.0.0.1}` |
| ActiveMQ 密码 | `manager` | `${ACTIVEMQ_PASSWORD:}` |
| OSS AccessKey | `KEY_KEY_KEY` / `SECRET_SECRET_SECRET` | `${ALIYUN_OSS_ACCESS_KEY_ID:}` / `${ALIYUN_OSS_ACCESS_KEY_SECRET:}` |

---

### 8. ✅ README 构建说明 — 已优化

**文件**: `README.md`

- 将 `./mvnw clean test` 改为 `./mvnw clean package -DskipTests`（测试需要数据库）
- 添加 `openssl rand -base64 32` 密钥生成提示
- 添加构建顺序说明（Maven 模块编排、npm workspaces、Docker Compose 服务编排）

---

## 三、未修复但已记录的问题

以下问题需要手动处理，无法通过代码修复：

### jeepay-sdk-java 版本号

版本号 `pls-1.3.0` 前缀不规范，Maven 中央仓库中验证 POM 存在（HTTP 200），但需确保 JAR 可下载。构建时若遇到依赖解析失败，需要确认该版本在所用 Maven 仓库中确实可用。

### 前端 Node.js 版本要求不一致

| 位置 | 要求 |
|------|------|
| `jeepay-ui/package.json` | `>=20 <23` |
| `jeepay-ui-cashier/package.json` | `>=18.0.0` |

建议统一为 `>=20`。

---

## 四、修复文件清单

| 文件 | 修复内容 |
|------|----------|
| `.env.example` | 移除 BOM |
| `docker-compose.yml` | 移除 BOM；MySQL healthcheck 改用 SELECT 1；UI healthcheck wget→curl |
| `docker/mysql/Dockerfile` | 移除 BOM |
| `docker/activemq/Dockerfile` | 移除 BOM |
| `.mvn/settings.xml` | 移除 BOM |
| `jeepay-ui/Dockerfile` | 移除 BOM；修复 ENV PORT 语法；healthcheck wget→curl |
| `conf/manager/application.yml` | 移除 BOM |
| `conf/payment/application.yml` | 移除 BOM |
| `conf/merchant/application.yml` | 移除 BOM |
| `conf/devCommons/config/application.yml` | 硬编码密码→环境变量；OSS key→环境变量 |
| `jeepay-z-codegen/pom.xml` | 添加父 POM；升级依赖；统一 Java 版本 |
| `docker/rocketmq/` | 删除废弃目录 |
| `README.md` | 优化构建说明和顺序 |

---

## 五、当前可用的安装步骤

```bash
# 1. 配置环境变量
cp .env.example .env
# 编辑 .env，设置所有 replace-with-* 值
# 生成 JWT 密钥: openssl rand -base64 32

# 2. 构建后端（跳过需要数据库的测试）
./mvnw clean package -DskipTests

# 3. 构建前端
npm --prefix jeepay-ui install
npm --prefix jeepay-ui run build

# 4. 启动全部服务
docker compose --env-file .env up --build
```
